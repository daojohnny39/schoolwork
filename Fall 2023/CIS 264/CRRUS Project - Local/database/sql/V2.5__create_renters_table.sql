CREATE TABLE Renters (
	RenterID int NOT NULL,
	RenterFirstName varchar(90),
	RenterLastName varchar(90),
	Password varchar(90),
	Phone varchar(50),
	Email varchar(255),
    PRIMARY KEY (RenterID)
);

